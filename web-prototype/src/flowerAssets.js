// public/flowers/flower_cycle.html is the shared p5.js sketch covering all four
// cycle phases via window.flowerSketch.setStage(key) — the keys are the same
// menstrual/follicular/ovulatory/luteal ids used in cycleData.js — plus live
// heartbeat/spo2/skin_temp/activity inputs (setHeartbeat/setBloodOxygen/
// setSkinTemp/setActivity), all 0-100-ish ranges matching the sensor schema.
export const FLOWER_CYCLE_SRC = "/flowers/flower_cycle.html";
